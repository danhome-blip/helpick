const startBtn = document.getElementById('start-btn');const outputDiv = document.getElementById('output');const recognition = new (window.SpeechRecognition || window.webkitSpeechRecognition)();recognition.lang = 'ro-RO';startBtn.addEventListener('click', () => { recognition.start(); startBtn.textContent = '🎙️ Ascult...';});recognition.onresult = (event) => { const text = event.results[0][0].transcript; outputDiv.textContent = `Ai spus: "${text}"`; startBtn.textContent = '🎤 Vorbește';};recognition.onerror = (event) => { outputDiv.textContent = 'Eroare: ' + event.error; startBtn.textContent = '🎤 Vorbește';};
